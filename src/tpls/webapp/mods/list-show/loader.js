var fs = require("fs");
var utils = require("../../../../utils");
var tmpl = utils.tmpl;
module.exports = function(mod, mp, env, config){
	if(!mp.details) mp.details = false; //click to enter the details of listview
	return 1;
}

